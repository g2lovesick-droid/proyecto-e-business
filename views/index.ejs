<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Meta Deportiva - Inicio</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">Meta Deportiva ⚽</a>
            <div class="d-flex">
                <% if (login) { %>
                    <span class="navbar-text text-white me-3">Hola, <strong><%= name %></strong></span>
                    <a href="/logout" class="btn btn-outline-danger btn-sm">Cerrar Sesión</a>
                <% } else { %>
                    <a href="/login" class="btn btn-outline-light btn-sm me-2">Login</a>
                    <a href="/register" class="btn btn-warning btn-sm">Registro</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="container mt-5">
        <h1 class="text-center mb-4">Catálogo de Productos</h1>
        
        <div class="row">
            <% if (data) { %>
                <% for(var i = 0; i < data.length; i++) { %>
                    <div class="col-md-4 mb-4">
                        <div class="card h-100 shadow-sm">
                            <div class="card-body text-center">
                                <h5 class="card-title"><%= data[i].nombre %></h5>
                                <p class="card-text text-muted"><%= data[i].descripcion %></p>
                                <a href="#" class="btn btn-primary">Ver Productos</a>
                            </div>
                        </div>
                    </div>
                <% } %>
            <% } %>
        </div>
    </div>

</body>
</html>